import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
    outerContainer: {
        background: 'gray',
        minWidth: '95vw',
        minHeight: '95vh',
    },
}));