import { makeStyles } from '@mui/styles';

export default makeStyles(() => ({
    outerContainer: {
        background: '#557155',
        minWidth: '100vw',
        maxWidth: '100vw',
        minHeight: '100vh',
        maxHeight: '100vh',
        padding: '0',
    },
}));