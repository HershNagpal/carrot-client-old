import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
    fenceContainer: {
        
    },
    fence: {
        height: '25px',
        width: '25px',  
    },
}));