import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
    outerContainer: {
        background: 'white',
        minWidth: '95vw',
        minHeight: '95vh',
    },
}));